<%- include('partials/header') -%>


          <div class="bg-base-200"> 
            <div class="flex flex-col w-full border-opacity-50">
            <!-- <div class="grid max-h-fit max-w-fit card bg-base-300 rounded-box place-items-left ml-10 pl-3 pr-3">
              <div>
                <h2 class="text-primary">User Info</h2>
                    <p><strong>User Name</strong>: <%= user.userName %></p>
                    <p><strong>Email</strong>: <%= user.email %></p> -->
                    <!-- <a href="/logout" class="col-3 btn btn-primary">Logout</a> -->
            </div>
          </div>
        <form action="/post/createPost" enctype="multipart/form-data" method="POST">
          <div class = "grid justify-items-center">
            <div class="card w-1/2 grid bg-base-200 justify-items-center mt-5">
               <textarea id="title" name="title" rows="4" class="card card-body mt-2.5 block p-2.5 w-3/4 text-sm text-gray-900 bg-gray-50 rounded-lg border border-gray-300 focus:ring-blue-500 focus:border-blue-500" placeholder="Ask a Question..."></textarea>
              <button type="submit" class="card card-actions card-normal btn btn-primary text-align-center w-1/3 mt-2.5 mb-2.5" value="Upload">Submit</button>
            </div>
          <div>
        </form>

              <!-- <%console.log(comments)%> -->
              <div class="min-h-fit bg-base-200 w-1/2">
              <div class="card grid justify-items-center mt-5">
                <ul class="row list-unstyled">
                  <% for(var j=0; j<posts.length; j++) {%>
                    <li class="col-6 flex justify-items-center mt-5 mb-5 border border-white">
                      <a href="/post/<%= posts[j]._id%>">
                        <h2><%= posts[j].title%></h2>
                      </a>
                    </li>
                  <% for(var i=0; i<comments.length; i++) {%>
                    <% if(comments[i].post.toString() == posts[j]._id.toString()) {%> 
                    <li class="col-6 justify-content-between mt-5">
                      <h2><%= comments[i].comment%></h2>
                    </li>
                    <% } %>
                   <% } %>
                  <% }%>
                </ul>

            </div>
          </div>  
<%- include('partials/footer') -%>